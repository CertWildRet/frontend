# Base URL of the cwr brain backend (Next.js exposes NEXT_PUBLIC_ vars to the client).
# Filler for now — wire to the real brain HTTP server once deployed.
NEXT_PUBLIC_BACKEND_URL=http://localhost:7878
